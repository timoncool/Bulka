# Examples Quality Report

**Дата анализа:** 2025-12-10
**Автор:** Claude Code Analysis
**Версия:** 1.0

---

## Executive Summary

**Текущее состояние:** Bulka AI имеет около **84 примеров** кода, распределённых по трём файлам. Количество достаточное, но **качество и покрытие требуют улучшения**.

**Ключевые проблемы:**
- ❌ Отсутствуют good/bad example pairs (критично для обучения)
- ❌ Нет примеров типичных ошибок и их исправления
- ❌ Недостаточно edge cases
- ❌ Нет примеров для каждого инструмента отдельно
- ❌ Слабое покрытие troubleshooting scenarios

**Рекомендация:** Добавить минимум 30-40 целевых примеров, сфокусированных на типичных ошибках и best practices.

---

## Current State

### Распределение примеров по файлам

| Файл | Количество примеров | Тип | Качество |
|------|---------------------|-----|----------|
| `system-prompt.ts` | ~15 | Quick reference | ⭐⭐⭐ Хорошо |
| `chat.ts` (CODE_EXAMPLES) | 9 | Жанровые шаблоны | ⭐⭐⭐⭐ Отлично |
| `knowledge.ts` | ~60 | База знаний | ⭐⭐⭐ Хорошо |
| **ИТОГО** | **~84** | - | **⭐⭐⭐** |

### Категоризация примеров

| Категория | Количество | Процент | Best Practice (v0/Claude) |
|-----------|------------|---------|---------------------------|
| Syntax (синтаксис, нотация) | ~25 | 30% | ✅ Достаточно |
| Sounds (звуки, семплы) | ~15 | 18% | ⚠️ Нужно больше |
| Effects (эффекты) | ~20 | 24% | ✅ Хорошо |
| Functions (модификаторы) | ~12 | 14% | ⚠️ Нужно больше |
| Examples (жанры, шаблоны) | 9 | 11% | ✅ Отлично |
| General (правила, tips) | ~3 | 4% | ❌ Критически мало |

### Покрытие по типам инструментов

| Tool | Есть примеры? | Количество | Оценка |
|------|---------------|------------|--------|
| `readCode` | ❌ Нет | 0 | Критично |
| `editCode` | ⚠️ Упоминание | 1-2 | Недостаточно |
| `appendCode` | ⚠️ Упоминание | 1 | Недостаточно |
| `setFullCode` | ✅ Да | 9 (жанры) | Хорошо |
| `playMusic` | ❌ Нет явных | 0 | Нужно |
| `searchDocs` | ❌ Нет | 0 | Критично |
| `getAvailablePacks` | ❌ Нет | 0 | **КРИТИЧНО** |
| `getBankSamples` | ❌ Нет | 0 | **КРИТИЧНО** |
| `highlightCode` | ❌ Нет | 0 | Нужно |
| `getConsole` | ❌ Нет | 0 | **КРИТИЧНО** |

---

## Quality Assessment

### Scoring Matrix (по критериям best practices)

| Критерий | Оценка (1-10) | Комментарий |
|----------|---------------|-------------|
| **Количество примеров** | 8/10 | ~84 примера достаточно |
| **Покрытие key scenarios** | 5/10 | Не хватает error handling, tool usage |
| **Good/Bad pairs** | 0/10 | ❌ **Критично: отсутствуют полностью** |
| **Edge cases** | 3/10 | Очень мало, только базовые случаи |
| **Best practices** | 6/10 | Есть, но не систематизированы |
| **Tool usage examples** | 2/10 | ❌ **Критично: почти отсутствуют** |
| **Error scenarios** | 1/10 | ❌ **Критично: почти нет** |
| **Progressive complexity** | 7/10 | От простого к сложному есть |
| **Real-world use cases** | 8/10 | Жанровые примеры отличные |
| **Commented code** | 4/10 | Мало комментариев в примерах |

**Общая оценка:** **4.4/10** (Удовлетворительно, требуется значительное улучшение)

### Сравнение с Best Practices

#### v0.dev (Vercel)
- ✅ **~50 примеров** с good/bad pairs
- ✅ Для каждого компонента показаны типичные ошибки
- ✅ Progressive disclosure (от простого к сложному)
- 📊 **Bulka:** Не хватает good/bad pairs и progressive disclosure

#### Claude Code (Anthropic)
- ✅ Примеры для **каждого инструмента** отдельно
- ✅ Edge cases и error handling
- ✅ Reasoning examples (как думать о задаче)
- 📊 **Bulka:** Отсутствуют примеры для tools, слабый error handling

#### Cursor (Anysphere)
- ✅ Search query examples (как правильно искать)
- ✅ Context selection examples
- ✅ Refactoring patterns
- 📊 **Bulka:** Нет примеров работы с searchDocs, getAvailablePacks

#### Windsurf (Codeium)
- ✅ Design patterns для типичных задач
- ✅ Workflow examples (последовательность действий)
- ✅ Multi-step task examples
- 📊 **Bulka:** Нет workflow examples, нет multi-step patterns

---

## Gap Analysis

### Критические пробелы (MUST HAVE)

#### 1. **Tool Usage Examples** (Критично!)
**Проблема:** Агент не знает КАК использовать инструменты.

**Отсутствуют примеры:**
- Как вызвать `readCode` перед редактированием
- Как использовать `editCode` для точечных изменений
- Когда использовать `searchDocs` vs `getAvailablePacks`
- Как обработать ошибку после `getConsole`
- Последовательность: `getAvailablePacks` → `getBankSamples` → `s("pack")`

**Влияние:** Агент выдумывает названия паков, не читает код перед изменением, не использует searchDocs.

#### 2. **Good/Bad Example Pairs** (Критично!)
**Проблема:** Агент не понимает ЧТО именно плохо.

**Отсутствуют:**
- ❌ ПЛОХО: `s("oneheart_ambient_sample_pack")` (выдуманный пак)
  ✅ ХОРОШО: `getAvailablePacks()` → найти пак → `s("реальный_пак")`

- ❌ ПЛОХО: Перезапись всего кода через `setFullCode` для изменения одной строки
  ✅ ХОРОШО: `readCode()` → `editCode(search, replace)`

- ❌ ПЛОХО: Не запускать `playMusic` после изменений
  ✅ ХОРОШО: `editCode()` → `playMusic()`

**Влияние:** Агент повторяет ошибки, которые мог бы избежать.

#### 3. **Error Handling Examples** (Критично!)
**Проблема:** Агент не знает КАК исправлять ошибки.

**Отсутствуют:**
- Пользователь говорит "не работает" → что делать?
- `editCode` не нашёл фрагмент → retry стратегия
- Синтаксическая ошибка в коде → как исправить
- Rate limit error → как обработать

**Влияние:** Агент теряется при ошибках, не может самостоятельно исправить.

#### 4. **Workflow Patterns** (Важно)
**Проблема:** Агент не знает ПОСЛЕДОВАТЕЛЬНОСТЬ действий.

**Отсутствуют:**
- Создание нового трека: `getExamples(жанр)` → адаптация → `setFullCode` → `playMusic`
- Добавление баса: `readCode` → `appendCode` или `editCode(stack)` → `playMusic`
- Поиск звука: `searchDocs("звук")` ИЛИ `getAvailablePacks` → `getBankSamples` → использование
- Отладка ошибки: `getConsole` → анализ → `readCode` → `editCode` → `playMusic`

**Влияние:** Агент не завершает задачу до конца, пропускает шаги.

### Важные пробелы (SHOULD HAVE)

#### 5. **Edge Cases**
- Пустой редактор → что делать?
- Очень длинный код → как редактировать?
- Пользователь выделил код → работа только с выделением
- Конфликтующие инструменты (bass + bass) → как обработать?

#### 6. **Advanced Patterns**
- Комбинирование техник (евклидовы ритмы + модуляция + эффекты)
- Использование переменных для переиспользования
- Создание сложных аранжировок
- Интеграция Hydra + музыка правильно

#### 7. **Common Mistakes**
- Забыл `playMusic` после изменений
- Использовал `setFullCode` вместо `editCode`
- Выдумал несуществующую функцию
- Не прочитал код перед редактированием

---

## Recommended Examples to Add

### Категория 1: Tool Usage (КРИТИЧНО - добавить в первую очередь)

#### Пример 1: readCode Before Edit
```markdown
# GOOD: Правильное использование readCode

## Scenario:
Пользователь: "Добавь бас"

## GOOD Approach:
1. **Шаг 1:** Вызываю readCode() чтобы увидеть текущий код
2. **Шаг 2:** Анализирую структуру (stack? один паттерн?)
3. **Шаг 3:** Выбираю appendCode (если нет stack) или editCode (если есть stack)
4. **Шаг 4:** Добавляю бас
5. **Шаг 5:** playMusic()

```javascript
// Текущий код (из readCode):
s("bd sd bd sd")

// Решение: appendCode (нет stack)
appendCode(`
note("c2 e2 g2 e2").s("sawtooth").lpf(400)
`)

// Результат:
s("bd sd bd sd")
note("c2 e2 g2 e2").s("sawtooth").lpf(400)
```

## BAD Approach (НЕ ДЕЛАЙ ТАК):
```javascript
// ❌ Не вызвал readCode - не знаю что в редакторе
// ❌ Сразу setFullCode - перезаписал всё
setFullCode(`
s("bd sd bd sd")
note("c2 e2 g2 e2").s("sawtooth").lpf(400)
`)
```

**Why bad:** Агент не знает что было в редакторе, мог потерять код пользователя.
```

#### Пример 2: searchDocs for Unknown Functions
```markdown
# GOOD: Использование searchDocs для неизвестных функций

## Scenario:
Пользователь: "Добавь vowel фильтр"
Агент не уверен что такое vowel фильтр.

## GOOD Approach:
1. **НЕ ВЫДУМЫВАЙ!** Если не уверен - вызови searchDocs
2. Вызываю searchDocs("vowel filter")
3. Получаю документацию: `.vowel("<a e i o u>")`
4. Применяю в коде

```javascript
// 1. Читаю код
readCode()

// 2. Ищу в документации
searchDocs("vowel filter")

// Результат поиска:
// "Vowel Filter (гласные звуки):
// s("sawtooth").vowel("<a e i o u>")"

// 3. Применяю
editCode({
  search: 's("sawtooth")',
  replace: 's("sawtooth").vowel("<a e i o u>")'
})

// 4. Запускаю
playMusic()
```

## BAD Approach (НЕ ДЕЛАЙ ТАК):
```javascript
// ❌ Выдумал функцию .vowelFilter() которой не существует
editCode({
  search: 's("sawtooth")',
  replace: 's("sawtooth").vowelFilter("a")' // WRONG!
})
// Это вызовет ошибку!
```

**Why bad:** Функция `.vowelFilter()` не существует. Нужно было searchDocs.
```

#### Пример 3: getAvailablePacks → getBankSamples Workflow
```markdown
# GOOD: Правильный поиск семплов

## Scenario:
Пользователь: "Используй 808 звуки"

## GOOD Approach - Полный workflow:
```javascript
// Шаг 1: Узнаю какие паки доступны
getAvailablePacks()

// Результат:
// Pack: "RolandTR808" (9 banks, type: sample, tag: drum-machines)
// Pack: "sparkway-drum-kit" (13 banks, type: sample, tag: drum-kits)

// Шаг 2: Вижу RolandTR808 - изучаю содержимое (опционально)
getBankSamples("RolandTR808_bd")

// Результат: ["bd0.wav", "bd1.wav", "bd2.wav"]

// Шаг 3: Использую в коде
readCode()
editCode({
  search: 's("bd sd")',
  replace: 's("bd sd").bank("RolandTR808")'
})
playMusic()
```

## BAD Approach (НЕ ДЕЛАЙ ТАК):
```javascript
// ❌ Выдумал название пака из головы
s("bd sd").bank("Roland808DrumMachine") // НЕ СУЩЕСТВУЕТ!

// ❌ Или ещё хуже:
s("808_bass_sample_pack") // ВЫДУМАЛ НАЗВАНИЕ!
```

**Why bad:** Названия паков выдуманы. Агент ДОЛЖЕН вызвать getAvailablePacks() ПЕРВЫМ.

**Critical Rule:** НИКОГДА не придумывай названия паков! ВСЕГДА getAvailablePacks() → проверка → использование.
```

#### Пример 4: getConsole for Error Debugging
```markdown
# GOOD: Отладка ошибки через getConsole

## Scenario:
Пользователь: "Не работает, ошибка какая-то"

## GOOD Approach:
```javascript
// Шаг 1: ПЕРВЫМ делом - getConsole для диагностики
getConsole()

// Результат консоли:
// "Error: ReferenceError: roomSize is not a function
//  at line 3: .roomSize(0.9)"

// Шаг 2: Анализирую ошибку - видю опечатку: roomSize → roomsize
// Шаг 3: Читаю код
readCode()

// Шаг 4: Исправляю опечатку
editCode({
  search: '.roomSize(0.9)',
  replace: '.roomsize(0.9)' // lowercase!
})

// Шаг 5: Запускаю
playMusic()
```

## BAD Approach (НЕ ДЕЛАЙ ТАК):
```javascript
// ❌ Не вызвал getConsole - не знаю в чём ошибка
// ❌ Пытаюсь угадать проблему
// ❌ Переписываю весь код наугад

setFullCode(`
// новый код наугад
s("bd sd")
`) // Может не исправить проблему
```

**Why bad:** Без getConsole агент слепой. Он ДОЛЖЕН видеть ошибку чтобы исправить.
```

#### Пример 5: editCode vs setFullCode Decision
```markdown
# GOOD: Выбор правильного инструмента редактирования

## Decision Tree:

### Используй setFullCode ТОЛЬКО если:
- ✅ Редактор **пуст** (readCode вернул "// Редактор пуст")
- ✅ Пользователь просит **новый трек с нуля**
- ✅ Пользователь просит **полностью переделать**

### Используй editCode если:
- ✅ Нужно **изменить часть кода**
- ✅ Нужно **заменить один элемент**
- ✅ Нужно **исправить ошибку**

### Используй appendCode если:
- ✅ Нужно **добавить новый элемент** к существующему
- ✅ Редактор НЕ пуст и нет stack()

## Example 1: Пустой редактор
```javascript
readCode() // Результат: "// Редактор пуст"

// ✅ ПРАВИЛЬНО: setFullCode (редактор пуст)
setFullCode(`
s("bd sd bd sd").bank("RolandTR808")
`)
```

## Example 2: Изменить один звук
```javascript
readCode()
// Результат:
// s("bd sd bd sd")

// ✅ ПРАВИЛЬНО: editCode (точечное изменение)
editCode({
  search: 'bd sd',
  replace: 'bd cp' // заменил sd на cp
})

// ❌ НЕПРАВИЛЬНО: setFullCode для одной замены
setFullCode(`
s("bd cp bd cp")
`) // Зачем перезаписывать весь код?
```

## Example 3: Добавить бас
```javascript
readCode()
// Результат:
// s("bd sd bd sd")

// ✅ ПРАВИЛЬНО: appendCode (добавить новый элемент)
appendCode(`
note("c2 e2 g2 e2").s("sawtooth").lpf(400)
`)

// ИЛИ если нужно в stack:
editCode({
  search: 's("bd sd bd sd")',
  replace: `stack(
  s("bd sd bd sd"),
  note("c2 e2 g2 e2").s("sawtooth").lpf(400)
)`
})
```

**Критическое правило:** editCode > appendCode > setFullCode (от меньшего к большему)
```

#### Пример 6: Parallel Tool Calls
```markdown
# GOOD: Параллельные вызовы инструментов для скорости

## Scenario:
Пользователь просит "создай техно трек с 909 звуками"

## GOOD Approach (параллельные вызовы):
```javascript
// Шаг 1: Параллельно собираю информацию
// ✅ Эти вызовы НЕ зависят друг от друга - можно параллельно!
[PARALLEL] {
  getExamples("techno"),
  searchDocs("RolandTR909"),
  readCode()
}

// Шаг 2: После получения результатов - создаю код
setFullCode(`
cpm(128);
stack(
  s("bd*4").bank("RolandTR909"),
  s("~ sd ~ sd").bank("RolandTR909"),
  s("hh*8").bank("RolandTR909").gain(0.5)
)
`)

// Шаг 3: Запускаю
playMusic()
```

## BAD Approach (последовательные вызовы):
```javascript
// ❌ Медленно: вызовы один за другим (5x медленнее!)
getExamples("techno")  // ждём...
// ... результат получен

searchDocs("RolandTR909")  // ждём...
// ... результат получен

readCode()  // ждём...
// ... результат получен

setFullCode(`...`)
```

**Why bad:** Каждый вызов ждёт предыдущий. Параллельные вызовы ускоряют в 3-5x!

## Когда МОЖНО параллельно:
- ✅ `searchDocs("bass")` + `readCode()` - независимые
- ✅ `getAvailablePacks()` + `readCode()` - независимые
- ✅ Несколько `searchDocs()` с разными запросами

## Когда НЕЛЬЗЯ (только последовательно):
- ❌ `readCode` → `editCode` - второй зависит от первого
- ❌ `editCode` → `playMusic` - playMusic после изменений
- ❌ `getAvailablePacks` → `getBankSamples(pack)` - второй нужен результат первого
```

### Категория 2: Good/Bad Pairs (КРИТИЧНО)

#### Пример 7: Complete Task vs Half-Done
```markdown
# Good/Bad: Завершение задачи полностью

## Scenario:
Пользователь: "Добавь хай-хэты"

## ❌ BAD - Незавершённая задача:
```javascript
// Агент изменил код, но НЕ ЗАПУСТИЛ
readCode()
appendCode('s("hh*8").gain(0.6)')
// Остановился! Не вызвал playMusic()
```
**Why bad:** Пользователь НЕ СЛЫШИТ результат. Задача не завершена.

## ✅ GOOD - Полное завершение:
```javascript
readCode()
appendCode('s("hh*8").gain(0.6)')
playMusic() // ✅ ЗАПУСТИЛ - задача завершена!
```
**Why good:** Пользователь сразу слышит результат. Задача доведена до конца.

**Критическое правило:** ВСЕГДА вызывай playMusic() после изменения кода!
```

#### Пример 8: Minimal Changes vs Full Rewrite
```markdown
# Good/Bad: Минимальные изменения vs перезапись

## Scenario:
Пользователь: "Замени бочку на клэп"

## ❌ BAD - Полная перезапись для одной замены:
```javascript
readCode()
// Результат: stack(s("bd sd"), s("hh*8"), note("c2").s("bass"))

// Агент ПЕРЕЗАПИСЫВАЕТ весь код для одной замены
setFullCode(`
stack(
  s("cp sd"), // заменил bd на cp
  s("hh*8"),
  note("c2").s("bass")
)
`) // ❌ Перезаписал 6 строк ради 1 изменения
```
**Why bad:** Неэффективно, риск потерять код, лишние токены.

## ✅ GOOD - Точечное изменение:
```javascript
readCode()
// Результат: stack(s("bd sd"), s("hh*8"), note("c2").s("bass"))

// Агент меняет только нужный фрагмент
editCode({
  search: 's("bd sd")',
  replace: 's("cp sd")'
}) // ✅ Изменил только 1 строку
```
**Why good:** Быстро, безопасно, минимум токенов.

**Критическое правило:** Используй editCode для точечных изменений, НЕ setFullCode!
```

#### Пример 9: Real Samples vs Invented Names
```markdown
# Good/Bad: Реальные семплы vs выдуманные названия

## Scenario:
Пользователь: "Используй ambient звуки"

## ❌ BAD - Выдумал название пака:
```javascript
// Агент ВЫДУМАЛ название из головы
s("ambient_atmosphere_pack") // ❌ НЕ СУЩЕСТВУЕТ!
s("oneheart_ambient_sample") // ❌ НЕ СУЩЕСТВУЕТ!
samples("ambient_sounds") // ❌ НЕ СУЩЕСТВУЕТ!
```
**Why bad:** Код НЕ РАБОТАЕТ. Пользователь получает ошибку.

## ✅ GOOD - Проверил через getAvailablePacks:
```javascript
// Шаг 1: Проверяю какие паки доступны
getAvailablePacks()

// Результат: нет специального "ambient" пака
// Есть: piano, vcsl, e-pianos, RolandTR808, sparkway-drum-kit...

// Шаг 2: Создаю ambient звук из доступных инструментов
setFullCode(`
note("c3 e3 g3 b3").s("sawtooth")
  .attack(0.5).release(2)
  .lpf(800)
  .room(0.8).roomsize(0.9)
  .delay(0.3)
`)
```
**Why good:** Код РАБОТАЕТ. Использует реальные инструменты.

**КРИТИЧЕСКОЕ ПРАВИЛО:**
- НИКОГДА не придумывай названия паков!
- ВСЕГДА вызывай getAvailablePacks() ПЕРВЫМ!
- Используй ТОЛЬКО то что вернул инструмент!
```

#### Пример 10: Trust User vs Argue
```markdown
# Good/Bad: Доверять пользователю vs спорить

## Scenario:
Пользователь: "Звук пропал, не работает"

## ❌ BAD - Агент спорит:
```
Агент: "Я запустил playMusic(), всё должно работать.
        Проверьте громкость на вашем устройстве."
```
**Why bad:**
- Агент НЕ ВИДИТ и НЕ СЛЫШИТ результат
- Пользователь ВИДИТ и СЛЫШИТ
- Спор = плохой UX

## ✅ GOOD - Агент доверяет и отлаживает:
```javascript
// Пользователь прав - ищу проблему
getConsole() // Смотрю что в консоли

// Результат: "Error: .room() is not a function.
//             Did you mean .room()?"

// Нашёл проблему - исправляю
readCode()
editCode({
  search: '.room()',
  replace: '.room(0.5)' // добавил параметр
})
playMusic()
```
**Why good:** Агент признаёт что пользователь ВИДИТ результат, исправляет проблему.

**КРИТИЧЕСКОЕ ПРАВИЛО:**
- ВСЕГДА доверяй пользователю
- Пользователь ВИДИТ результат, ты - НЕТ
- Если пользователь говорит "не работает" → getConsole() и исправляй
```

### Категория 3: Workflow Patterns (ВАЖНО)

#### Пример 11: Create New Track Workflow
```markdown
# Workflow: Создание нового трека с нуля

## Scenario:
Пользователь: "Создай hip-hop бит"

## Complete Workflow:
```javascript
// Шаг 1: Проверяю состояние редактора
readCode()
// Результат: "// Редактор пуст" ИЛИ есть код

// Шаг 2: Параллельно собираю информацию
[PARALLEL] {
  getExamples("hiphop"), // получаю шаблон hip-hop
  searchDocs("RolandTR808") // узнаю про 808
}

// Шаг 3: Создаю код на основе шаблона
setFullCode(`
cpm(90); // hip-hop tempo

stack(
  // 808 бочка с паттерном
  s("bd ~ ~ bd ~ ~ sd ~").bank("RolandTR808").gain(1.2),

  // хай-хэты
  s("hh*8").bank("RolandTR808").gain(0.6),

  // 808 бас (суб-бас)
  note("c1 ~ c1 ~").s("sawtooth").lpf(200).gain(0.8)
)._scope()
`)

// Шаг 4: Запускаю
playMusic()

// Шаг 5: Краткое объяснение
"Создал hip-hop бит на 90 BPM с классическими 808 звуками и саб-басом."
```

## Key Points:
1. ✅ Проверил состояние редактора (readCode)
2. ✅ Использовал getExamples для вдохновения
3. ✅ Проверил документацию (searchDocs)
4. ✅ Создал полный код (setFullCode для пустого редактора)
5. ✅ Запустил музыку (playMusic)
6. ✅ Дал краткое объяснение
```

#### Пример 12: Add Bass to Existing Track Workflow
```markdown
# Workflow: Добавление баса к существующему треку

## Scenario:
Пользователь: "Добавь бас"

## Complete Workflow:
```javascript
// Шаг 1: ОБЯЗАТЕЛЬНО читаю текущий код
readCode()

// Результат (пример):
// s("bd sd bd sd")

// Шаг 2: Анализирую структуру
// - НЕТ stack() → нужно либо appendCode либо создать stack
// - Простой паттерн → выбираю appendCode

// Шаг 3: Добавляю бас
appendCode(`
note("c2 e2 g2 e2").s("sawtooth").lpf(400)._pianoroll()
`)

// Шаг 4: Запускаю
playMusic()

// Результат:
// s("bd sd bd sd")
// note("c2 e2 g2 e2").s("sawtooth").lpf(400)._pianoroll()
```

## Alternative: Если есть stack
```javascript
readCode()
// Результат:
// stack(
//   s("bd sd bd sd"),
//   s("hh*8")
// )

// Нужно добавить в stack → использую editCode
editCode({
  search: 'stack(\n  s("bd sd bd sd"),\n  s("hh*8")\n)',
  replace: `stack(
  s("bd sd bd sd"),
  s("hh*8"),
  note("c2 e2 g2 e2").s("sawtooth").lpf(400)._pianoroll()
)`
})

playMusic()
```

## Key Decision Points:
1. ✅ Читаю код ПЕРВЫМ (readCode)
2. ✅ Анализирую структуру (stack? простой паттерн?)
3. ✅ Выбираю правильный инструмент:
   - Нет stack → appendCode
   - Есть stack → editCode для добавления в stack
4. ✅ Запускаю (playMusic)
```

#### Пример 13: Find and Use Sound Workflow
```markdown
# Workflow: Поиск и использование звука

## Scenario:
Пользователь: "Используй звуки UK garage"

## Complete Workflow:
```javascript
// Шаг 1: Ищу информацию о UK garage
searchDocs("UK garage")

// Результат: "sparkway-drum-kit отлично подходит для UK Garage"

// Шаг 2: Параллельно проверяю доступность и читаю код
[PARALLEL] {
  getAvailablePacks(), // проверяю что sparkway есть
  readCode() // смотрю текущий код
}

// Результат getAvailablePacks:
// Pack: "sparkway-drum-kit" (13 banks, type: sample, tag: drum-kits)

// Шаг 3: Изучаю содержимое пака
getBankSamples("spk_kick")

// Результат: ["bomb.wav", "burial.wav", "calvin.wav", ...]

// Шаг 4: Создаю UK garage трек
setFullCode(`
cpm(130); // UK garage tempo

stack(
  // шаффл-ритм для UK garage
  s("spk_kick ~ [~ spk_kick] ~"),
  s("~ spk_snare ~ spk_snare"),
  s("spk_hat*8").gain(0.5),

  // саб-бас
  s("spk_808").lpf(500).gain(0.8)
)
`)

// Шаг 5: Запускаю
playMusic()
```

## Key Points:
1. ✅ Искал документацию (searchDocs)
2. ✅ Проверил доступность пака (getAvailablePacks)
3. ✅ Изучил содержимое (getBankSamples)
4. ✅ Использовал ТОЛЬКО реальные звуки
5. ✅ Запустил результат (playMusic)
```

#### Пример 14: Debug Error Workflow
```markdown
# Workflow: Отладка ошибки

## Scenario:
Пользователь: "Ошибка, не работает"

## Complete Workflow:
```javascript
// Шаг 1: ПЕРВЫМ делом - диагностика
getConsole()

// Результат консоли:
// "Error: TypeError: .roomSize is not a function
//  at line 5: .roomSize(0.9)"

// Шаг 2: Анализирую ошибку
// .roomSize не существует → должно быть .roomsize (lowercase)

// Шаг 3: Читаю код чтобы найти проблему
readCode()

// Результат:
// s("bd sd")
//   .delay(0.5)
//   .room(0.5)
//   .roomSize(0.9) // ← ошибка тут

// Шаг 4: Исправляю опечатку
editCode({
  search: '.roomSize(0.9)',
  replace: '.roomsize(0.9)' // lowercase!
})

// Шаг 5: Запускаю
playMusic()

// Шаг 6: Объясняю
"Исправил опечатку: .roomSize → .roomsize (lowercase). Теперь работает!"
```

## Alternative: Синтаксическая ошибка
```javascript
getConsole()
// Результат: "SyntaxError: Unexpected token '}' at line 3"

readCode()
// Результат:
// stack(
//   s("bd sd")
// } // ← должна быть скобка )

editCode({
  search: '}',
  replace: ')'
})

playMusic()
```

## Key Points:
1. ✅ getConsole ПЕРВЫМ при ошибке
2. ✅ Анализирую ошибку
3. ✅ readCode для локализации проблемы
4. ✅ editCode для исправления
5. ✅ playMusic для проверки
6. ✅ Объясняю что исправил
```

#### Пример 15: Work with Selected Code
```markdown
# Workflow: Работа с выделенным кодом

## Scenario:
Пользователь выделил фрагмент: `s("bd sd bd sd")`
Пользователь: "Сделай быстрее"

## Complete Workflow:
```javascript
// Контекст от системы:
// selectedCode: 's("bd sd bd sd")'
// fullCode: 'stack(\n  s("bd sd bd sd"),\n  s("hh*8")\n)'

// Шаг 1: Фокусируюсь на ВЫДЕЛЕННОМ коде
// Пользователь хочет изменить ТОЛЬКО его

// Шаг 2: Применяю изменение к выделенному фрагменту
editCode({
  search: 's("bd sd bd sd")',
  replace: 's("bd sd bd sd").fast(2)' // добавил .fast(2)
})

// Шаг 3: Запускаю
playMusic()

// Шаг 4: Объясняю
"Ускорил бочку и снейр в 2 раза."
```

## Key Points:
1. ✅ Если есть selectedCode → работаю ТОЛЬКО с ним
2. ✅ Не трогаю остальной код без необходимости
3. ✅ Фокус на выделенном фрагменте
4. ✅ Краткое объяснение что изменил
```

### Категория 4: Edge Cases (ВАЖНО)

#### Пример 16: Empty Editor
```markdown
# Edge Case: Пустой редактор

## Scenario:
Пользователь: "Добавь реверб"
Редактор пуст.

## Правильная обработка:
```javascript
// Шаг 1: Читаю код
readCode()
// Результат: "// Редактор пуст"

// Шаг 2: Анализирую ситуацию
// Редактор пуст → невозможно добавить реверб к несуществующему звуку

// Шаг 3: Спрашиваю уточнение
"Редактор пуст. Хочешь создать трек с реverbом или сначала добавить звуки?"

// ИЛИ проактивно создаю простой трек с реverbом:
setFullCode(`
s("bd sd bd sd").room(0.5).roomsize(0.8)
`)
playMusic()

"Создал простой бит с реverbом. Хочешь добавить что-то ещё?"
```

## Why important:
- Агент не может добавить эффект к пустому редактору
- Нужно либо спросить, либо создать базовый трек
```

#### Пример 17: Very Long Code
```markdown
# Edge Case: Очень длинный код

## Scenario:
Код в редакторе 200+ строк.
Пользователь: "Замени бочку на клэп"

## Правильная обработка:
```javascript
// Шаг 1: Читаю код (будет обрезан системой до 2000 символов)
readCode()

// Шаг 2: Ищу нужный фрагмент
// Использую editCode для точечного поиска

// Шаг 3: Если НЕ НАШЁЛ - пытаюсь снова с другим поиском
editCode({
  search: 's("bd',
  replace: 's("cp'
})

// Если ошибка "not found" → пробую другой вариант
editCode({
  search: 'bd',
  replace: 'cp'
})

// Максимум 3 попытки, потом спрашиваю пользователя
```

## Why important:
- Длинный код может быть обрезан
- Нужна стратегия поиска с fallback
```

#### Пример 18: Conflicting Instructions
```markdown
# Edge Case: Конфликтующие инструкции

## Scenario:
Код: `note("c2").s("bass").lpf(400)`
Пользователь: "Добавь бас"

## Правильная обработка:
```javascript
readCode()
// Результат: note("c2").s("bass").lpf(400)

// Анализ: уже ЕСТЬ бас
// Два варианта:
// 1. Добавить ВТОРОЙ бас (layer)
// 2. Уточнить у пользователя

// Проактивный подход:
appendCode(`
note("c2 e2 g2 e2").s("sawtooth").lpf(600)._pianoroll() // второй бас
`)
playMusic()

"Добавил второй басовый слой. Хочешь настроить баланс?"

// ИЛИ уточняющий подход:
"У тебя уже есть бас. Добавить второй слой или заменить существующий?"
```

## Why important:
- Избегаем конфликтов
- Показываем что мы понимаем контекст
```

#### Пример 19: Unknown Function Requested
```markdown
# Edge Case: Пользователь просит несуществующую функцию

## Scenario:
Пользователь: "Добавь pitch bend"

## Правильная обработка:
```javascript
// Шаг 1: Не уверен что такое pitch bend в Strudel
searchDocs("pitch bend")

// Результат: "Ничего не найдено"

// Шаг 2: Пробую альтернативные поисковые запросы
[PARALLEL] {
  searchDocs("pitch"),
  searchDocs("bend"),
  searchDocs("note modulation")
}

// Результат: нашёл .add(), .note(), .freq()

// Шаг 3: Предлагаю альтернативу
"В Strudel нет pitch bend как в MIDI, но можно использовать .add() для изменения высоты:"

setFullCode(`
note("c4 e4 g4")
  .s("sine")
  .add(sine.range(-1, 1).slow(2)) // плавное изменение высоты
`)
playMusic()
```

## Why important:
- Не все DAW-функции есть в Strudel
- Нужно искать альтернативы
- Объяснять пользователю
```

#### Пример 20: Rate Limit Error
```markdown
# Edge Case: Rate limit от API

## Scenario:
API возвращает 429 Too Many Requests

## Правильная обработка:
```
// Система автоматически retry с exponential backoff
// Агент получает либо успех, либо финальную ошибку

// Если финальная ошибка - объясняю пользователю:
"Извини, сервер перегружен. Подожди 30 секунд и попробуй снова.
 Или упрости запрос - используй меньше инструментов за раз."
```

## Why important:
- Прозрачность с пользователем
- Полезные советы (подождать, упростить)
```

### Категория 5: Advanced Patterns (ДОЛЖНО БЫТЬ)

#### Пример 21: Euclidean Rhythm + Modulation
```markdown
# Advanced: Евклидовы ритмы с модуляцией

```javascript
// Комбинирование евклидова ритма с модуляцией фильтра
s("bd(3,8)") // 3 удара на 8 шагов
  .bank("RolandTR808")
  .lpf(sine.range(200, 2000).slow(4)) // модуляция фильтра
  .gain(0.9)

// С эффектами
s("cp(5,8,2)") // 5 ударов на 8 шагов, смещение на 2
  .room(0.4)
  .delay(0.25)
```

**Когда использовать:** Сложные полиритмические паттерны
```

#### Пример 22: Variables for Reusability
```markdown
# Advanced: Переменные для переиспользования

```javascript
// Сохраняем аккордовую прогрессию
const progression = chord("<Em C Am D>");

// Используем в разных инструментах
stack(
  // Бас - корневые ноты
  progression.rootNotes(2).s("square").lpf(700),

  // Пад - полные аккорды
  n("<4 2 5 3>").set(progression).voicing().add(12).s("piano").room(0.5),

  // Арпеджио
  progression.arpeggio().s("sine")._pianoroll()
)
```

**Когда использовать:** Сложные аранжировки с переиспользованием паттернов
```

#### Пример 23: Complex Arrangement
```markdown
# Advanced: Сложная аранжировка

```javascript
cpm(116/2);

const progression = chord("<Em C Am D>");

// Секция интро
const intro = stack(
  s("bd sd bd sd").gain(0.8)._scope(),
  s("hh*8").gain(0.6),
  n("<4 2 5 3>").set(progression).voicing().add(12).s("piano").room(0.5)._pianoroll()
);

// Секция куплет
const verse = stack(
  s("bd ~ sd ~"),
  s("hh*8").gain(0.5),
  progression.rootNotes(2).s("square").lpf(700)
);

// Секция припев
const chorus = stack(
  s("bd*2 sd bd sd"),
  s("hh*16").gain(0.7),
  progression.arpeggio().s("sine").delay(0.25),
  n("<4 2 5 3>").set(progression).voicing().s("piano").room(0.7)
);

// Аранжировка: 4 такта интро, 8 тактов куплет, 8 тактов припев
arrange([4, intro], [8, verse], [8, chorus]);
```

**Когда использовать:** Полноценные треки с структурой
```

#### Пример 24: Hydra + Music Integration
```markdown
# Advanced: Правильная интеграция Hydra

```javascript
// КРИТИЧНО: Hydra ВСЕГДА в начале кода!
await initHydra()

// Визуализация с синхронизацией по музыке (H)
osc(8, 0.1, 0.8)
  .color(0.3, 0.1, 1)
  .kaleid(5)
  .scale(H("0.8 1.2 0.9 1.5")) // H() синхронизация с музыкой
  .modulate(voronoi(4, 0.5), 0.2)
  .rotate(0.2, 0.05)
  .out()

// Музыка ПОСЛЕ Hydra
stack(
  s("bd sd bd sd"),
  s("hh*8").gain(0.6),
  note("c2 e2 g2 e2").s("sawtooth").lpf(400)
)._scope()
```

**Критические правила:**
1. ✅ Hydra ВСЕГДА в начале (иначе перекроет код)
2. ✅ Используй H() для синхронизации с музыкой (не с микрофоном)
3. ✅ await initHydra() обязательно
```

#### Пример 25: Interactive Sliders
```markdown
# Advanced: Интерактивные слайдеры для live-выступления

```javascript
stack(
  // Барабаны
  s("bd sd bd sd"),

  // Бас с управляемым фильтром
  note("c2 e2 g2 e2")
    .s("sawtooth")
    .lpf(slider(800, 200, 4000)) // пользователь крутит cutoff
    .gain(slider(0.8, 0, 1)),

  // Пад с управляемым реverbом
  note("c3 e3 g3").s("piano")
    .room(slider(0.3, 0, 1))
    .roomsize(slider(0.7, 0, 1))
    .delay(slider(0.2, 0, 0.5))
)
```

**Когда использовать:** Live coding выступления, интерактивные сессии
```

### Категория 6: Common Mistakes (ДОЛЖНО БЫТЬ)

#### Пример 26: Forgot playMusic
```markdown
# Common Mistake: Забыл playMusic

## ❌ MISTAKE:
```javascript
// Пользователь: "Добавь бас"
readCode()
appendCode('note("c2").s("bass")')
// ЗАБЫЛ playMusic() - музыка не запустилась!
```

## ✅ CORRECT:
```javascript
readCode()
appendCode('note("c2").s("bass")')
playMusic() // ✅ ВСЕГДА в конце!
```

**Как избежать:** Мысленный checklist: изменил код → playMusic()
```

#### Пример 27: Used setFullCode for Small Change
```markdown
# Common Mistake: setFullCode вместо editCode

## ❌ MISTAKE:
```javascript
// Пользователь: "Сделай бочку тише"
readCode() // 50 строк кода
setFullCode(`... все 50 строк с одним изменением .gain(0.5)...`) // ПЛОХО!
```

## ✅ CORRECT:
```javascript
readCode()
editCode({
  search: 's("bd")',
  replace: 's("bd").gain(0.5)'
}) // изменил только 1 строку
playMusic()
```

**Как избежать:** Если изменение точечное → editCode, не setFullCode
```

#### Пример 28: Invented Function Name
```markdown
# Common Mistake: Выдумал несуществующую функцию

## ❌ MISTAKE:
```javascript
// Пользователь: "Добавь chorus эффект"
s("bd sd").chorus(0.5) // ❌ .chorus() НЕ СУЩЕСТВУЕТ!
```

## ✅ CORRECT:
```javascript
// Шаг 1: Не уверен - ищу в документации
searchDocs("chorus")

// Результат: "Используй .delay() + .room() для chorus-подобного эффекта"

// Шаг 2: Применяю реальные функции
s("bd sd")
  .delay(0.1).delayfeedback(0.3)
  .room(0.4)
```

**Как избежать:** Не уверен в функции? searchDocs() ПЕРВЫМ!
```

#### Пример 29: Didn't Read Code Before Edit
```markdown
# Common Mistake: Не прочитал код перед редактированием

## ❌ MISTAKE:
```javascript
// Пользователь: "Добавь хэты"
// Агент НЕ вызвал readCode
appendCode('s("hh*8")') // Может уже есть хэты!
```

## ✅ CORRECT:
```javascript
// Шаг 1: ВСЕГДА readCode первым
readCode()

// Результат: s("bd sd bd sd") - хэтов нет

// Шаг 2: Безопасно добавляю
appendCode('s("hh*8").gain(0.6)')
playMusic()
```

**Как избежать:** readCode() ВСЕГДА первым перед изменениями!
```

#### Пример 30: Placeholder Comments
```markdown
# Common Mistake: Плейсхолдеры в коде

## ❌ MISTAKE:
```javascript
setFullCode(`
stack(
  s("bd sd bd sd"),
  // ... остальной код ...  ❌ ПЛЕЙСХОЛДЕР!
  note("c2").s("bass")
)
`)
```

## ✅ CORRECT:
```javascript
// ВСЕГДА пишу ПОЛНЫЙ код, без плейсхолдеров
setFullCode(`
stack(
  s("bd sd bd sd"),
  s("hh*8").gain(0.6),
  note("c2 e2 g2 e2").s("bass").lpf(400),
  note("c3 e3 g3").s("piano").room(0.4)
)
`)
```

**Как избежать:** Никогда не используй "...", "// rest of code", комментарии-заглушки
```

---

## Implementation Priority

### P0 - КРИТИЧНО (реализовать НЕМЕДЛЕННО)

Эти примеры решают **критические проблемы** которые ломают UX:

1. ✅ **Пример 3:** `getAvailablePacks → getBankSamples` workflow
   - **Проблема:** Агент выдумывает названия паков
   - **Влияние:** Код не работает, пользователь получает ошибку
   - **Где добавить:** `system-prompt.ts` в секцию `<tools>`

2. ✅ **Пример 4:** `getConsole` for error debugging
   - **Проблема:** Агент не может отлаживать ошибки
   - **Влияние:** Пользователь застревает с ошибкой
   - **Где добавить:** `system-prompt.ts` в секцию `<error_handling>`

3. ✅ **Пример 1:** `readCode` before edit
   - **Проблема:** Агент редактирует код вслепую
   - **Влияние:** Может потерять код пользователя
   - **Где добавить:** `system-prompt.ts` в секцию `<workflow>`

4. ✅ **Пример 9:** Real samples vs invented names (Good/Bad pair)
   - **Проблема:** Агент не понимает ПОЧЕМУ нельзя выдумывать
   - **Влияние:** Повторяющиеся ошибки
   - **Где добавить:** `knowledge.ts` как новый chunk

5. ✅ **Пример 7:** Complete task vs half-done (Good/Bad pair)
   - **Проблема:** Агент забывает playMusic()
   - **Влияние:** Пользователь не слышит результат
   - **Где добавить:** `system-prompt.ts` в секцию `<workflow>`

### P1 - ОЧЕНЬ ВАЖНО (реализовать в течение недели)

6. ✅ **Пример 2:** `searchDocs` for unknown functions
   - **Проблема:** Агент выдумывает функции
   - **Где добавить:** `system-prompt.ts` в секцию `<tools>`

7. ✅ **Пример 5:** `editCode vs setFullCode` decision tree
   - **Проблема:** Агент перезаписывает код без нужды
   - **Где добавить:** `system-prompt.ts` в секцию `<workflow>`

8. ✅ **Пример 8:** Minimal changes vs full rewrite (Good/Bad)
   - **Где добавить:** `knowledge.ts` как новый chunk

9. ✅ **Пример 11-14:** Workflow patterns (все 4)
   - **Проблема:** Агент не знает последовательность действий
   - **Где добавить:** `knowledge.ts` как category "workflows"

10. ✅ **Пример 10:** Trust user vs argue (Good/Bad)
    - **Проблема:** Агент спорит с пользователем
    - **Где добавить:** `system-prompt.ts` в секцию `<critical_rules>`

### P2 - ВАЖНО (реализовать в течение 2 недель)

11-20. ✅ **Примеры 16-20:** Edge cases (все 5)
    - **Где добавить:** `knowledge.ts` как category "edge_cases"

21-25. ✅ **Примеры 21-25:** Advanced patterns (все 5)
    - **Где добавить:** `chat.ts` в CODE_EXAMPLES (новые категории)

26-30. ✅ **Примеры 26-30:** Common mistakes (все 5)
    - **Где добавить:** `knowledge.ts` как category "common_mistakes"

### P3 - ЖЕЛАТЕЛЬНО (долгосрочная цель)

31. **Пример 6:** Parallel tool calls
    - **Где добавить:** `system-prompt.ts` в секцию `<parallel_tools>`

32-40. **Дополнительные примеры:**
    - Комбинации техник
    - Genre-specific tips
    - Performance optimization
    - Visualization best practices

---

## Metrics for Success

### До улучшений (текущее состояние):
- Good/Bad pairs: **0**
- Tool usage examples: **2-3**
- Error handling examples: **1-2**
- Workflow patterns: **0**
- Edge cases: **0**
- Overall quality score: **4.4/10**

### После P0 (критические примеры):
- Good/Bad pairs: **5**
- Tool usage examples: **5**
- Error handling examples: **3**
- Workflow patterns: **1**
- Edge cases: **0**
- Overall quality score: **6.5/10** (+2.1)

### После P1 (очень важные):
- Good/Bad pairs: **8**
- Tool usage examples: **7**
- Error handling examples: **4**
- Workflow patterns: **5**
- Edge cases: **0**
- Overall quality score: **8.0/10** (+3.6)

### После P2 (все рекомендации):
- Good/Bad pairs: **13**
- Tool usage examples: **7**
- Error handling examples: **6**
- Workflow patterns: **5**
- Edge cases: **5**
- Overall quality score: **9.0/10** (+4.6)

### Целевые показатели (best-in-class):
- Good/Bad pairs: **15+** (как v0.dev)
- Tool usage examples: **10+** (как Claude Code)
- Error handling examples: **8+**
- Workflow patterns: **8+** (как Windsurf)
- Edge cases: **10+**
- Overall quality score: **9.5/10**

---

## Recommended Structure Changes

### 1. Добавить новую секцию в system-prompt.ts
```typescript
<tool_usage_examples>
## ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ ИНСТРУМЕНТОВ

[Примеры 1-7 здесь]
</tool_usage_examples>
```

### 2. Расширить knowledge.ts новыми категориями
```typescript
// Добавить новые категории:
category: 'workflows' | 'edge_cases' | 'common_mistakes' | 'good_bad_pairs'
```

### 3. Добавить новые примеры в chat.ts CODE_EXAMPLES
```typescript
'euclidean': `...`, // Пример 21
'variables': `...`, // Пример 22
'complex-arrangement': `...`, // Пример 23
// и т.д.
```

### 4. Создать отдельный файл для Good/Bad pairs
```typescript
// website/src/repl/ai-agent/good-bad-examples.ts
export const GOOD_BAD_EXAMPLES = [
  {
    scenario: "Добавить бас",
    bad: { code: "...", reason: "..." },
    good: { code: "...", reason: "..." }
  },
  // ...
];
```

---

## Next Steps

1. **Immediate (сегодня):**
   - Добавить P0 примеры (5 штук) в system-prompt.ts
   - Провести A/B тест с пользователями

2. **This week:**
   - Добавить P1 примеры (10 штук)
   - Обновить knowledge.ts с новыми категориями
   - Замерить улучшение метрик

3. **This month:**
   - Добавить все 30 рекомендованных примеров
   - Создать систему автоматической оценки качества примеров
   - Собрать feedback от пользователей

4. **Long-term:**
   - Continuous improvement на основе реальных ошибок агента
   - Добавление новых примеров по мере развития Strudel
   - Создание интерактивной документации с примерами

---

## Appendix A: Example Template

Для единообразия новых примеров используй этот шаблон:

```markdown
# [Good/Bad/Workflow/Edge Case]: Краткое название

## Scenario:
[Что хочет пользователь]

## [GOOD/BAD/Complete] Approach:
```javascript
// Код с комментариями
```

## Why [good/bad/important]:
[Объяснение]

## Key Points:
- ✅ Point 1
- ✅ Point 2
- ❌ What to avoid
```

---

**Конец отчёта**

*Создано: 2025-12-10*
*Версия: 1.0*
*Следующий review: после реализации P0*
